import React from "react";
import "./footer.scss";
import classNames from "classnames";

const Footer = () => {
  return <footer className={classNames("container", "footer")}></footer>;
};

export default Footer;
